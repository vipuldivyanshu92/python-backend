# from pymongo import Connection
#import

#mongo db create
# connection = Connection()
# db=connection.Giffie.users_trans

AUTH_ID=''

def user_online_ping(auth_id,db,nick):
  
  
def user_get_msgs_ping(auth_id,db,nick,iemi):
  
  
def user_name_registered(auth_id,db,nick): # this is to check if the freinds name entered registered or not
  
  
def user_is_contact_registered(auth_id,db,nick,phone_num,email):# this is to check if the user is contact is registered or not
  
  
def user_post(auth_id,db,nick_timestamp,phone_num):
  
def user_receive_confirm(auth_id,db,id,iemi,systimestamp)
